import type { GlobalOptions } from "../index.js";
import { notImplemented } from "../index.js";

export function setupGit(_positionals: string[], options: GlobalOptions): void {
  notImplemented("setup-git", options.json);
}
