import type { GlobalOptions } from "../index.js";
import { notImplemented } from "../index.js";

export function fmt(_positionals: string[], options: GlobalOptions): void {
  // reads stdin, writes formatted text to stdout
  notImplemented("fmt", options.json);
}
