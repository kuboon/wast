name: Publish Devcontainer Image

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: Commit SHA used for both amd64 and arm64 image tags
        required: true
        type: string
      publish_latest:
        description: Also update :latest to this multi-arch manifest
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Build amd64 and publish multi-arch
    runs-on: ubuntu-latest
    env:
      IMAGE: ghcr.io/kuboon/wast/wast-dev-container
      SOURCE_SHA: ${{ inputs.source_sha }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: short-sha
        run: |
          set -euo pipefail
          short_sha="$(echo "${SOURCE_SHA}" | cut -c1-12)"
          echo "SHORT_SHA=${short_sha}" >> "$GITHUB_OUTPUT"

      - name: Verify arm64 tag exists
        env:
          SHORT_SHA: ${{ steps.short-sha.outputs.SHORT_SHA }}
        run: |
          set -euo pipefail
          docker buildx imagetools inspect "${IMAGE}:arm64-${SHORT_SHA}" >/dev/null

      - name: Build and push amd64 image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE }}:amd64-${{ steps.short-sha.outputs.SHORT_SHA }}
            ${{ env.IMAGE }}:amd64-latest


      - name: Publish multi-arch manifest
        env:
          SHORT_SHA: ${{ steps.short-sha.outputs.SHORT_SHA }}
        run: |
          set -euo pipefail
          AMD64_TAG="${IMAGE}:amd64-${SHORT_SHA}"
          ARM64_TAG="${IMAGE}:arm64-${SHORT_SHA}"
          SHA_TAG="${IMAGE}:${SHORT_SHA}"

          if [[ "${{ inputs.publish_latest }}" == "true" ]]; then
            docker buildx imagetools create \
              -t "${SHA_TAG}" \
              -t "${IMAGE}:latest" \
              "${AMD64_TAG}" \
              "${ARM64_TAG}"
          else
            docker buildx imagetools create \
              -t "${SHA_TAG}" \
              "${AMD64_TAG}" \
              "${ARM64_TAG}"
          fi

      - name: Inspect published manifest
        env:
          SHORT_SHA: ${{ steps.short-sha.outputs.SHORT_SHA }}
        run: docker buildx imagetools inspect "${IMAGE}:${SHORT_SHA}"
